pipeline {
    agent any
    stages {
      stage('Install dependencies') {
            steps {
                sh 'git --version'
            }
        }
            stage("Deploy"){
              steps {
                echo "Deploying the application....."
              }
            }
          
    }
}
