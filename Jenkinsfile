pipeline {
    agent any
    stages {
      stage('Install dependencies') {
            steps {
                sh 'node --version'
            }
        }
            stage("Deploy"){
              steps {
                echo "Deploying the application....."
              }
            }
          
    }
}
