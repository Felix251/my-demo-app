pipeline {
    agent any
    stages {
      stage('Install dependencies') {
            steps {
                sh 'npm --version'
            }
        }
            stage("Deploy"){
              steps {
                echo "Deploying the application....."
              }
            }
          
    }
}
